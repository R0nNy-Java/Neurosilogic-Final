spring.application.name=Neurosilogic Final
server.port=8443
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=nurselogic123
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=nurselogic

# ══ Configuración para MySQL (servidor local MySQL Workbench) ══
spring.datasource.url=jdbc:mysql://localhost:3306/neurosilogic_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=K30adrianp.

# JPA / Hibernate para MySQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Configuración de Sesiones HTTP (invalida sesiones almacenadas en memoria al reiniciar el servidor)
spring.session.store-type=none
server.servlet.session.persistent=false
server.servlet.session.timeout=30m

# ══ Correo (Gmail SMTP) para envío de contraseña temporal en "Olvidé mi contraseña" ══
# Reemplaza el valor de spring.mail.password con la "contraseña de aplicación" de Gmail
# (Cuenta de Google > Seguridad > Contraseñas de aplicaciones), NUNCA la contraseña normal
# de la cuenta. NO commitear este archivo con la contraseña real puesta.
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=inventarioguest@gmail.com
spring.mail.password=bmyb kqkh lwku cmwn
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
